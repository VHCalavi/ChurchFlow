{
  "name": "churchflow-monorepo",
  "private": true,
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "db:migrate": "pnpm --filter @churchflow/database db:migrate",
    "db:migrate:prod": "pnpm --filter @churchflow/database db:migrate:prod",
    "db:generate": "pnpm --filter @churchflow/database db:generate",
    "db:seed": "pnpm --filter @churchflow/database db:seed",
    "db:seed:prod": "pnpm --filter @churchflow/database db:seed:prod",
    "deploy:api": "pnpm db:generate && vercel link --project churchflow-api --yes && vercel build --prod --yes && vercel deploy --prebuilt --prod --yes",
    "deploy:admin": "NEXTAUTH_URL=\"https://churchflow-admin.vercel.app\" NEXT_PUBLIC_API_URL=\"https://churchflow-api.vercel.app\" vercel link --project churchflow-admin --yes && vercel build --prod --yes && vercel deploy --prebuilt --prod --yes",
    "deploy:landing": "vercel link --project churchflow-landing --yes && vercel build --prod --yes && vercel deploy --prebuilt --prod --yes",
    "deploy:all": "pnpm deploy:api && pnpm deploy:admin && pnpm deploy:landing"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^1.13.0",
    "typescript": "^5.4.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "reactflow": "^11.11.4"
  }
}
